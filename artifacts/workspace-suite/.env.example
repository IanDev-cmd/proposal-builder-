# Shared 4-digit Nexus team PIN. Vite inlines this at build time.
# Render: set VITE_NEXUS_TEAM_PASSWORD on weott-quote-builder (sync: false).
# Local: copy to .env.local (gitignored). Do not commit the real PIN.
VITE_NEXUS_TEAM_PASSWORD=
